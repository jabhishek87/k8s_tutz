apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
spec:
  ports:
  - port: 32003
    nodePort: 32003
    targetPort: http-port
    protocol: TCP
  selector:
    app: wordpress
  type: NodePort
